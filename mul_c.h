/**
 * Multiplication of 256-bit number with 64-bit constant
 *
 * @param a		256-bit number stores the first multiplicand
 * @param c 	64-bit number stores the second multiplicand
 * @param null	empty parameter
 * @param r		64*5-bit number stores the result
 */
void mul_c(unsigned long *a, unsigned long c, unsigned long *null, unsigned long *r);
